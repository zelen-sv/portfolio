export default function ({ app, store }) {
  // app.router.beforeEach((to, from, next) => {
  //   store.dispatch('loader/showLoader')
  //   setTimeout (() => next(), 800)
  // });

  // app.router.afterEach((to, from) => {
  //   setTimeout (() => store.dispatch('loader/hideLoader'), 800)
  // });
}
